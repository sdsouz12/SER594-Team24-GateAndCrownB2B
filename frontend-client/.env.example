# Dev: leave empty so /api is proxied to the backend (vite.config.js → localhost:8080).
# Production build served without proxy: set full API origin, e.g. https://api.example.com
# VITE_API_BASE_URL=

# Optional: override proxy target when running `npm run dev`
# API_PROXY_TARGET=http://localhost:8080
